name: Release Creation

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Get version from tag
      - name: Get Version
        id: get-version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      # Update module.json version
      - name: Update module.json
        run: |
          # Ensure version in module.json matches tag
          sed -i "s/\"version\": \".*\"/\"version\": \"${{ steps.get-version.outputs.version }}\"/" module.json

      # Create zip file
      - name: Create Zip File
        run: |
          # Ensure the zip excludes unnecessary files
          zip -r coffee-pub-scribe.zip \
            module.json \
            LICENSE \
            README.md \
            scripts/ \
            styles/ \
            templates/ \
            lang/ \
            images/ \
            sounds/ \
            -x "*.git*" -x "*.github*" -x "*node_modules*" -x "*.vscode*"

      # Create release
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: |
            coffee-pub-scribe.zip
            module.json
          draft: false
          prerelease: false
          name: Release ${{ steps.get-version.outputs.version }}
          body: |
            Release version ${{ steps.get-version.outputs.version }}
            
            ## What's Changed
            * Automated release from tag v${{ steps.get-version.outputs.version }}
            
            For full changes, see the [changelog](CHANGELOG.md). 